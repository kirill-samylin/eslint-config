# eslint-config

## Install

``` npm install kirill-samylin/eslint-config ```

## Usage

Config can be added to .eslintrc

```JS
module.exports = {
  extends: [
    'kirill-samylin/eslint-config',
  ],
}
```
